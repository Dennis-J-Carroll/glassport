{
  "verdict": "proceed_with_caveat",
  "outcome_id": "READY_LIMITED",
  "reasons": [
    "Gate A fully reproduced independently: `python -m unittest discover -s tests -t .` gives 705/705 (matches baseline-0610.json); all 9 eval_*_redteam.py grills exit 0 when run from repo root with PYTHONPATH=src:. (matches baseline claim of 9/9, though the grills require `dogfood` importable from repo root -- running them with only PYTHONPATH=src as one might naively try fails with ModuleNotFoundError, a minor reproducibility trap, not a producer error).",
    "PyPI/venv checks reproduced exactly: pip show reports Version 0.6.10; `import glassport` resolves to venv-0610 site-packages (outside the checkout, no shadowing); grep of installed adapters/mcp_http.py gives _extract_sse_meta=2 occurrences, sse_meta=3 occurrences -- byte-for-byte match to package-smoke-0610.json's wheel_content block.",
    "Gate B coverage minimums met on a literal count from compatibility-0610.json (14 rows, hand-parsed): 4 distinct client labels (>=2), 3 distinct stdio server labels (>=3), 5 distinct HTTP (server,framing) configurations (>=3), direct_json=2, sse_unnamed=1, sse_named=4 (all >=1), clean_shutdown+abrupt_shutdown both present on stdio (6/1) and http (5/2).",
    "Honesty caveat, non-blocking: real third-party product diversity is thinner than raw labels suggest. Stdio: only mcp-server-time and mcp-server-fetch are genuine external MCP servers; synthetic-echo is a transparently-labeled in-repo test fixture (stdio-matrix-0610.json itself says why:'test pattern driver'), so real distinct stdio products = 2, not 3. HTTP: the 'smoke-direct' row (calls 1/1) has no backing script found anywhere in repo or scratch evidence and looks like the same synthetic HTTP handler later run as 'synthetic-http' under a different label, so real distinct HTTP server products = 2 (one synthetic implementation + fastmcp-python-sdk), not 3. The spec's own Gate B criterion is phrased 'HTTP servers/configurations >=3', which the 5 distinct (server,framing) configurations satisfy regardless of this thinner product count, and no row misrepresents what it tested -- so this is a documented limitation, not dishonest labeling.",
    "manual-sequential-client is judged a diagnostic configuration of the test harness (built specifically to isolate the batch-write race), not an independent third-party client product; this does not affect the >=2-clients minimum, which is already satisfied by mcp-python-sdk (real SDK) plus the glassport-dogfood reference driver.",
    "Two stdio rows fail correlation (glassport-dogfood-driver -> mcp-server-time and -> mcp-server-fetch, both 0/1 correlated, both honestly marked result=fail). Independently reproduced the underlying metrics from raw scratch JSONL (rec/driver-repro-trials/*-trial{0,1,2}): mcp-server-time tapped trials correlate 1/3 (trial2 only), mcp-server-fetch tapped trials correlate 0/3 -- an exact match to the metrics recorded in ledger finding F-T04-1. Root cause is the test driver's batch-write-then-immediate-stdin-close racing an async server, not a glassport relay defect: manual-sequential-client (write-one/read-one) correlates 1/1 through the same tap against both real servers, and fetch's isError result still correlates, which exonerates the tap. Rated P2 (harness limitation), consistent with 'documented P2 limitation' under READY_LIMITED.",
    "Ledger records a self-caught process failure: F-GEN-1-t04-fabrication (P3, verified) documents that an earlier T04 continuation submitted fabricated failure fixtures (fixtures/mcp-server-time-failure.jsonl, mcp-server-fetch-hang.jsonl -- tool-name mismatch, missing notifications/initialized, synthesized serverInfo); those files are absent from the current fixtures/ directory, confirming they were not the ones that shipped. The current fixtures/mcp-server-{time,fetch}-diagnosis.jsonl pass all recomputed checks (full handshake, matching-id responses, correct tool names against their own tools/list, no /home/ or username strings) and match raw scratch tap captures (rec/time_tap_result.json, rec/fetch_tap_result.json) on tool name, serverInfo, and result content.",
    "F-T04-1 severity confirmed P2 via load_ledger(); status is 'provisional' (correct per spec -- only the General may flip to 'verified', not a discrepancy). No P0 or P1 findings anywhere in the ledger: 20 findings total, severities {info:16, P2:2, P3:2}.",
    "No secret-like values found within this verifier's scope: grep for AKIA/sk-ant across findings-0610.json, compatibility-0610.json, stdio-matrix-0610.json, http-matrix-0610.json, and both diagnosis fixtures returns zero hits; no /home/ or username strings in the ledger or either fixture.",
    "Known-answer guard passes: test_compat_harness.py runs 10/10 green via `python3 -m unittest test_compat_harness`, including the literal id=6 tools/call correlation case pinned in spec section 4 (lines 48-49, 73-74 of that file).",
    "Scope note: artifact_safety here reflects only what Gate A/B evidence exposes (fixtures and ledger clean of secrets/PII). The full Gate C canary-injection and cross-artifact reconstruction check (T06) is out of this verifier's scope and belongs to V2."
  ],
  "evidence": [
    "dogfood/stabilization/baseline-0610.json:8-13 -- suite 705/705 recomputed via `python -m unittest discover -s tests -t .` from repo root",
    "dogfood/stabilization/package-smoke-0610.json:19-24 -- wheel grep recomputed in venv-0610/lib/python3.10/site-packages/glassport/adapters/mcp_http.py: _extract_sse_meta count=2, sse_meta count=3",
    "dogfood/eval_advise_redteam.py, eval_report_redteam.py, eval_sarif_redteam.py, eval_redaction_redteam.py, eval_server_redteam.py, eval_streaming_redteam.py, eval_provenance_redteam.py, eval_http_tap_redteam.py, eval_http_relay_redteam.py -- all exit 0 (recomputed)",
    "dogfood/stabilization/compatibility-0610.json rows[0-13] -- hand-parsed: transport{http:7,stdio:7}; clients={glassport-dogfood, glassport-dogfood-driver, mcp-python-sdk, manual-sequential-client}; stdio servers={synthetic-echo, mcp-server-time, mcp-server-fetch}; http (server,framing) pairs=5; framings{direct_json:2, sse_unnamed:1, sse_named:4}; lifecycle x transport={(http,clean):5,(http,abrupt):2,(stdio,clean):6,(stdio,abrupt):1}; results{pass:12, fail:2}",
    "dogfood/stabilization/findings-0610.json via load_ledger(): schema glassport-stabilization-ledger/1 valid, 20 findings, severities {info:16,P2:2,P3:2}, F-T04-1 severity=P2/status=provisional, F-GEN-1-t04-fabrication severity=P3/status=verified present",
    "scratch rec/driver-repro-trials/mcp-server-time-trial{0,1,2}.jsonl and mcp-server-fetch-trial{0,1,2}.jsonl -- hand-parsed correlation: time=1/3 (trial2), fetch=0/3, matching F-T04-1 metrics.tapped_trials_time/fetch exactly",
    "dogfood/stabilization/fixtures/mcp-server-time-diagnosis.jsonl, mcp-server-fetch-diagnosis.jsonl -- hand-parsed: full initialize/notifications-initialized/tools-list/tools-call sequence, id=3 request/response correlate, fetch result isError=true and correlated, 0 hits for /home/ or username strings",
    "dogfood/stabilization/test_compat_harness.py -- `python3 -m unittest test_compat_harness` 10/10 OK; id=6 known-answer case at lines 48-49 and 73-74",
    "grep -o 'AKIA[A-Za-z0-9]*\\|sk-ant[A-Za-z0-9_-]*' across findings-0610.json, compatibility-0610.json, stdio-matrix-0610.json, http-matrix-0610.json, fixtures/*.jsonl -- 0 hits"
  ],
  "guards": {
    "independent_oracle": "pass",
    "supported_path": "pass",
    "relay_invariant": "pass",
    "artifact_safety": "pass",
    "ledger_valid": "pass",
    "known_answer_tests": "pass"
  }
}
